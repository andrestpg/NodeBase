<%- include('../part/header.ejs') %> 

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <div class="w-100 d-flex justify-content-between align-items-center">
                                    <h4 class="card-title"><%= title %></h4>
                                    <div>
                                        <button class="btn btn-success" id="reload"><i class="fa fa-refresh"></i></button>
                                        <button class="btn btn-info" data-toggle="modal" data-target="#addModal">Tambah</button>
                                    </div>
                                </div>
                            </div>
                            <div class="card-body" id="targetContent">
                            </div>
                        </div>
                    </div>
				</div>

<%- include(`../${path}/modal.ejs`) %>
<%- include('../part/footer.ejs') %> 